# Atlas agent guide

Atlas is a multi-tenant workflow platform. Follow the architecture, product, security, testing, and design docs. Use pnpm. Never leak data. Write good code. Run tests. The API uses tenant scoping and the worker uses leases. The web app follows DESIGN.md. Keep documentation current.

