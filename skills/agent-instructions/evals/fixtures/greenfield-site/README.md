# Greenfield Site

This Next.js site publishes Markdown from `content/`.

Use Bun:

```bash
bun install
bun run build
```

Source images remain canonical. `bun run images:optimize` writes smaller generated variants under `public/images/_optimized/`.

