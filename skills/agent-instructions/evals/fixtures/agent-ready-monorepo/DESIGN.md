# Atlas interface design

This file is the authority for UI interaction, hierarchy, accessibility, and visual decisions.

- Every async action exposes pending, success, empty, and error states.
- All interactive controls remain keyboard reachable with visible focus.
- Run `pnpm design:lint` and capture the changed responsive states.

