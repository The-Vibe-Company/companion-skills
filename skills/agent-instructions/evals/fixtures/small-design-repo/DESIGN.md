# Interface contract

This is the authority for the page's interaction hierarchy, responsive behavior, accessibility, and visual examples. Changes to `src/` run `npm run design:lint` and inspect mobile and desktop states.

