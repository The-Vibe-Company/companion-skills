# CLAUDE.md

You are a world-class helpful engineer working on Sample Control Plane.

## Product overview

This is a TypeScript application with many folders. The API lives in `apps/api`, the website lives in `apps/web`, and packages live in `packages`. Write clean, maintainable, elegant code. Think carefully before editing. Always be helpful.

## Status

The application has not been implemented yet. Assume every path below is a future target and create it if missing.

## Commands

- Run `npm test` after every change.
- Run `npm run lint` after every change.
- Run `pnpm verify:change` when you remember.

## Personal setup

- Ask release questions by emailing maintainer@example.invalid.
- Product plans live at https://www.notion.so/00000000000000000000000000000000.
- Use workspace id `11111111-1111-4111-8111-111111111111`.

## Repository map

- `apps/api` contains the API.
- `apps/web` contains the website.
- `apps/worker` contains the worker.
- `packages/core` contains core code.
- `packages/db` contains database code.
- `packages/contracts` contains contracts.
- `docs` contains documentation.
- `scripts` contains scripts.

## Rules

- Always write clean code.
- Never make mistakes.
- Follow best practices.
- Read the whole repository before changing anything.
- Always run every test in the monorepo.
- Personal resources are creator-private and admins have no override.
- Never persist secret plaintext.
- Untrusted code runs in a sandbox.

